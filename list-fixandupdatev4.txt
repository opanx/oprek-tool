LIST UPDATE (FITUR BARU YANG HARUS DITAMBAH)
============================================================

[ ] 1. ELF FULL HEADER PARSER — tampilkan semua field e_type, e_machine, e_entry, e_phoff, e_shoff, e_flags, e_ehsize, e_phentsize, e_phnum, e_shentsize, e_shnum, e_shstrndx + deteksi arsitektur

[ ] 2. PROGRAM HEADER (SEGMENT) VIEWER — tampilkan semua segment PT_LOAD, PT_DYNAMIC, PT_INTERP, PT_NOTE dengan kolom Type, Offset, VAddr, PAddr, FileSz, MemSz, Flags, Align + warna berdasarkan permission (executable hijau, writable merah, read-only biru)

[ ] 3. SECTION HEADER VIEWER — tampilkan semua section .text, .data, .rodata, .bss, .symtab, .strtab, .dynsym, .dynstr, .got, .plt + filter berdasarkan tipe (executable/writable/debug)

[ ] 4. SYMBOL TABLE (.symtab & .dynsym) — tampilkan semua symbol (fungsi, variabel, objek) dengan kolom Num, Value, Size, Type, Bind, Vis, Ndx, Name + warna FUNC hijau, OBJECT kuning, NOTYPE abu-abu + search & filter + click symbol langsung ke alamat di disassembler

[ ] 5. DYNAMIC SECTION (.dynamic) — tampilkan DT_NEEDED, DT_SONAME, DT_RPATH, DT_RUNPATH, DT_INIT, DT_FINI + library dependencies

[ ] 6. RELOCATION (.rel.dyn, .rel.plt) — tampilkan offset, type, symbol, addend + tipe relokasi (R_ARM_*, R_AARCH64_*, R_X86_64_*)

[ ] 7. GOT & PLT VIEWER — tampilkan GOT entries dan PLT stub

[ ] 8. SHELL SCRIPT PARSER — deteksi shebang, ekstrak metadata (interpreter, line count, size), tampilkan struktur (shebang, komentar, variable, function, payload)

[ ] 9. BINARY EXTRACTOR DARI SHELL SCRIPT — deteksi binary terenkapsulasi (base64, hex, uudecode, gzip, tar, dd), ekstrak otomatis ke /tmp/, tampilkan posisi offset, ukuran, tipe kompresi

[ ] 10. SHELL SCRIPT DEOBFUSCATOR — deteksi obfuscation (base64, hex, ROT13, caesar, XOR, shell escape), decode otomatis, export ke file baru

[ ] 11. SHELL SCRIPT PATCHER — edit URL, IP, key, token, path, ganti command (curl → echo), patch if condition, save ke file baru

[ ] 12. SHELL SCRIPT ANALYZER — tampilkan semua command, URL/IP/domain, key/token/license, dependency, flow graph sederhana

[ ] 13. DISASSEMBLER (CAPSTONE NATIVE) — support ARM32/ARM64/x86/x86_64, tampilkan address + hex bytes + mnemonic + operands + warna (opcode cyan, mnemonic hijau, operand putih)

[ ] 14. DISASSEMBLE SECTION — disassemble seluruh .text + function boundaries dari symbol table

[ ] 15. DISASSEMBLE FUNCTION — pilih fungsi dari symbol table, tampilkan disassembly + control flow (basic block)

[ ] 16. DISASSEMBLE ADDRESS — input alamat → disassemble 10 instruksi + XREF

[ ] 17. DECOMPILER (PSEUDO-C) — pake r2ghidra atau Ghidra native, tampilkan pseudo-C dari fungsi yang dipilih (variable names, function calls, loop, if-else)

[ ] 18. MEMORY DUMP ANALYZER — baca raw dump, deteksi alamat base, tampilkan region (text, data, stack, heap), ekstrak string dan pointer

[ ] 19. PATTERN SEARCH ADVANCED — cari hex pattern (7F 45 4C 46), text pattern, regex (URL, IP, email, GUID), instruction pattern (mov x0, x1)

[ ] 20. PATCH INSTRUCTION (ELF) — ubah opcode di alamat tertentu, pilih instruction baru dari dropdown (NOP, RET, JMP), preview perubahan, save ke file baru

[ ] 21. PATCH BRANCH (ELF) — deteksi conditional (JNE, JE, JZ, JNZ, CBNZ, CBZ), ubah jadi unconditional JMP / NOP / RET 0

[ ] 22. AUTO PATCH LOGIN BYPASS (ELF) — cari string "Wrong", "Invalid", "Login Failed", cari XREF, trace branch, sarankan patch JNE→JMP / RET 0, one-click apply

[ ] 23. PATCH STRING (ELF) — ganti string di .rodata/.data (ASCII/UTF-16/Unicode), preview sebelum-sesudah, sisanya diisi NOP/null

[ ] 24. STRING DEOBFUSCATE (ELF) — scan semua string di .rodata/.strtab, deteksi Base64/Hex/XOR/Unicode/URL/Reverse/ROT13, tampilkan raw + decoded, tombol Apply to File

[ ] 25. XOR BRUTE FORCE — brute force key 0x00-0xFF untuk string yang di-XOR, tampilkan hasil terbaik (entropy rendah), auto detect key

[ ] 26. DETEKSI ANTI-DEBUG — cek TracerPid, ptrace, /proc/self/maps, frida detection (file /proc/self/fd), gdb detection (/proc/self/status)

[ ] 27. PATCH ANTI-DEBUG — patch TracerPid check (return 0), ptrace check (return 0), frida detection (NOP), gdb detection (NOP), one-click bypass

[ ] 28. DETEKSI PACKER — UPX (cek string "UPX!"), Themida (entropy tinggi), OLLVM (banyak NOP + CF flattening), deteksi berdasarkan entropy + compressed section

[ ] 29. UNPACKER OTOMATIS — UPX (jalanin upx -d), manual unpack (dump memory saat runtime)

[ ] 30. CONTROL FLOW GRAPH (CFG) — tampilkan graph interaktif dari fungsi yang dipilih, node = basic block, edge = jump/conditional, warna hijau true branch, merah false, biru unconditional, zoom in/out, drag node, click node → disassembly

[ ] 31. CROSS-REFERENCE (XREF) VIEWER — tampilkan XREF TO (ke mana fungsi manggil) dan XREF FROM (siapa manggil), click langsung lompat ke alamat

[ ] 32. FUNCTION LIST + FILTER — tampilkan semua fungsi (.symtab/.dynsym), kolom Nama/Alamat/Size/Type/Binding, search + filter, click → disassembly

[ ] 33. DISASSEMBLER PANEL INTERAKTIF — tampilkan disassembly dengan warna, click alamat → set bookmark, right click → Follow in Hex View / Patch Instruction / Set Breakpoint (Frida), tampilkan address + hex bytes + instruction

[ ] 34. STRING WINDOW (IDA STYLE) — extract semua string (.rodata/.strtab), kolom Address/String/Type, search + filter, click string → XREF viewer

[ ] 35. BOOKMARK & ANNOTATION — bookmark alamat + kasih nama custom (contoh: "login_check"), simpan di Room DB, tampilkan di sidebar, export/import ke JSON

[ ] 36. EXPORT REPORT (IDA STYLE) — export ke HTML (warna + interaktif), JSON (buat diproses lagi), TXT (plain text), include function list, strings, xrefs, patches applied, bookmarks

[ ] 37. SESSION SAVE / LOAD — save semua state analisis (file yang dibuka, bookmark, patches, notes), load session → lanjutin analisis dari terakhir

[ ] 38. DIFF TOOL — bandingkan 2 binary, highlight perbedaan (hex + offset)

[ ] 39. ENTROPY ANALYZER — deteksi enkripsi/kompresi di section tertentu

[ ] 40. STRING ENCRYPTOR — enkripsi semua string di binary (buat proteksi sendiri)

============================================================
LIST FIX (BUG YANG HARUS DIPERBAIKI)
============================================================

[ ] 1. NATIVE LIBRARY (.so) — gagal load di beberapa device. Fix: tambah try-catch + fallback di NativeLib.kt, bundel semua ABI (arm64-v8a, armeabi-v7a, x86_64)

[ ] 2. COROUTINE CRASH — user pindah screen tengah proses. Fix: tambah try-catch + CancellationException handling di semua ViewModel + timeout 60 detik

[ ] 3. FILE BESAR (>100MB) — OutOfMemoryError. Fix: implementasikan streaming I/O (bukan load all bytes ke memory) + batasi max file 200MB + loading progress

[ ] 4. HEX EDITOR UNDO/REDO — history ilang setelah edit besar. Fix: batasi history 50 langkah, simpan di memory, tampilkan peringatan kalau melebihi limit

[ ] 5. PATCH EDITOR — error saat apply patch di offset tertentu. Fix: validasi offset (harus dalam range file), handle buffer overflow, tampilkan pesan error detail

[ ] 6. DISASSEMBLER — masih fallback "native library not loaded". Fix: integrasikan Capstone native (libcapstone.so) + wrapper JNI-nya

[ ] 7. DEOBFUSCATE AUTO-SCAN — detect tapi ga bisa apply patch. Fix: tambah fungsi patchStringInFile() di Deobfuscator.kt + batch patch + progress bar

[ ] 8. HOOK GENERATOR — ga auto-detect exported functions dari .so. Fix: tambah ElfSymbolExtractor.kt (parse .dynsym) + dropdown pilihan function di FridaHookScreen.kt + edit script langsung

[ ] 9. SHELL SCRIPT EXTRACTOR — ga deteksi binary yang di-encode. Fix: support base64 -d, xxd -r, uudecode, gunzip, tar -x, deteksi otomatis dari shebang/encoding

[ ] 10. APK ANALYZER — crash di file APK gede. Fix: parsing streaming + batasi size + tampilkan progress

[ ] 11. STRINGS EXTRACTOR — beberapa string gak kebaca. Fix: support UTF-16, Unicode, dan string dengan null terminator

[ ] 12. TERMINAL — command tertentu gak jalan. Fix: tambah built-in command (file, xxd, strings, objdump, readelf) pake BusyBox atau binary bawaan

[ ] 13. ELF PARSER — error di file stripped. Fix: handle missing section/symbol table, tampilkan pesan "stripped, info terbatas"

[ ] 14. OBFUSCATE — encode XOR ga jalan. Fix: perbaiki logic XOR di obfuscate.c, tambah test case

[ ] 15. PATTERN DETECTOR — false positive. Fix: tambah confidence score (0-100), filter threshold 70%, kasih user opsi override

[ ] 16. FILE INFO — magic bytes ga terdeteksi. Fix: tambah database magic bytes (ELF, DEX, APK, ZIP, PE, Mach-O, SH, PY, JAR, AAR, SO, O, ARC)

[ ] 17. HASH CALCULATOR — hang di file gede. Fix: implementasikan hashing streaming (MD5, SHA-1, SHA-256, SHA-512, CRC32)

[ ] 18. SEARCH — pencarian lama di file gede. Fix: implementasikan Boyer-Moore atau Aho-Corasick + tampilkan progress

[ ] 19. DARK THEME — beberapa teks ga kebaca. Fix: cek kontras (background vs text), tambah opsi custom color

[ ] 20. PERFORMANCE — UI freeze saat load file gede. Fix: gunakan Coroutines + Flow, tampilkan shimmer effect / loading indicator

[ ] 21. MEMORY LEAK — setelah buka banyak file. Fix: implementasikan Bitmap.recycle() untuk image, clear cache tiap 5 menit, gunakan WeakReference untuk context