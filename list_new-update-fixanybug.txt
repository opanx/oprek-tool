You are an expert Android reverse engineering engineer and senior Kotlin/C++ developer specializing in mobile RE tools (IDA Pro, Ghidra, Binary Ninja, radare2, JEB, apktool level).

Your task is to **completely overhaul and upgrade** the project https://github.com/opanx/oprek-tool (OprekTool) into a professional-grade, production-ready Android Reverse Engineering Toolkit that rivals desktop tools like IDA Pro / Ghidra as much as possible on a mobile device.

### Primary Goals (in order of priority):
1. **Fix ALL existing bugs** — eliminate every compilation error, crash, ANR, memory leak, UI glitch, incorrect parsing, broken navigation, missing imports, type mismatches, JNI crashes, and logic errors. Make the app fully stable and crash-free.
2. **Dramatically improve code quality** — clean architecture, proper error handling, null safety, resource management, and performance.
3. **Add powerful new features** inspired by IDA Pro, Ghidra, Binary Ninja, and radare2 that still make sense on Android (offline, limited RAM/CPU).

### Mandatory Technical Requirements:
- Keep modern stack: Kotlin + Jetpack Compose (Material 3) + MVVM + StateFlow + Coroutines + Native C++ (JNI)
- Target SDK 35, Min SDK 26
- Smooth 120fps UI where possible
- Strict memory management (no OOM on large files, progressive loading, streaming analysis)
- Fully offline
- Proper dark theme + good contrast
- All heavy work must run on background threads / Dispatchers.Default / native threads
- Write clean, documented, maintainable code
- Add proper logging (Timber or equivalent)
- Handle edge cases (corrupted files, truncated binaries, non-standard ELF/DEX/PE, large files >100MB)

### Bugs to Fix First (must be done thoroughly):
- All compilation errors and missing imports
- Content lambda / Compose scope issues
- JNI crashes and native memory leaks
- Incorrect ELF / PE / DEX header parsing
- Broken auto-detect features
- UI freezes and ANRs
- Incorrect patching (especially bulk & advanced patch)
- Disassembler accuracy and architecture detection
- Memory Analyzer / entropy / packer detection false positives
- File loading and caching bugs
- Navigation and back-stack issues
- Output export failures

### High-Priority New Features to Add (make it closer to IDA/Ghidra):

**Core Analysis Engine**
- Proper Capstone-based or custom disassembler for ARM32, ARM64, x86, x86-64 with correct instruction decoding, cross-references, and basic control-flow graph (CFG) visualization
- Function recognition & naming (heuristics + known library signatures)
- Cross-references (XREFs) viewer — who calls / who is called
- Basic decompiler output (pseudo-C) for simple functions (even if limited)
- Strings with cross-references and auto-classification (URL, path, crypto constant, etc.)
- Import/Export table analysis + PLT/GOT resolution
- Relocation and dynamic section deep analysis
- Symbol recovery and demangling (C++ names)

**Advanced Binary Features**
- Interactive Hex Editor with live disassembly view (synced)
- Graph view (CFG) for functions (using Canvas or Compose graphics)
- Signature scanning (YARA-like or simple pattern DB for common libraries/packers/protectors)
- Better packer/protector detection + unpacking hints (UPX, Themida, VMProtect, OLLVM, etc.)
- Diff tool that shows instruction-level differences + visual side-by-side
- Memory map / segment viewer with permissions
- Entropy graph + visualization
- Automatic detection of obfuscation techniques (string encryption, control flow flattening, etc.)

**Android-Specific Power Features**
- Full APK analysis: AndroidManifest deep parse, resources.arsc, assets, DEX class hierarchy browser, Smali viewer/editor, native library list with architecture
- DEX → Smali decompilation + basic Smali → Java pseudo code
- Multi-DEX support
- Frida script generator (advanced — method hooks, native hooks, anti-debug bypass templates)
- Logcat with live filtering + Frida integration readiness
- Root detection & anti-analysis detection suite

**Patching & Modification**
- Advanced binary patcher with live preview
- NOP / RET / JMP / call patching with automatic size calculation
- String replacement with length adjustment / padding
- Code cave finder + injector
- Batch patch from script / list
- Generate .so patch or APK patch package

**UI/UX & Productivity**
- Proper project system (save analysis session, bookmarks, comments, renamed functions)
- Global search across all views (strings, code, hex, symbols)
- Multi-tab / multi-pane layout (hex + disasm + decompiler)
- Dark/Light theme + customizable accent
- Recent files + project history with thumbnails
- Export full report (Markdown + HTML + JSON)
- Keyboard shortcuts / gesture support for power users
- Performance monitor (memory usage, analysis time)

**Native Layer Improvements**
- Rewrite/strengthen all parsers (ELF, PE, DEX, Mach-O if possible) to be robust
- Add Capstone or similar disassembly engine properly via NDK
- Fast pattern matching and scanning engine in C++
- Memory-efficient file mapping (mmap style)

### Implementation Rules:
- Prefer incremental, working commits (fix first → core engine → new features)
- Every new feature must have proper error handling and loading states
- Do not break existing working tools
- Keep the app usable on mid-range devices (Snapdragon 7-series / Dimensity 700+)
- Add unit tests for critical parsers if possible
- Update README comprehensively after major changes (features, screenshots description, build instructions, known limitations)

### Deliverables:
1. Fix all current bugs completely
2. Implement as many high-value features above as possible (prioritize core analysis + disassembler + XREF + graph + better patching)
3. Make the architecture clean and ready for future expansion
4. Ensure the app builds successfully via `./gradlew assembleDebug` and `assembleRelease`
5. Update README with new feature list and version bump (e.g. v3.0 or v5.0)

Start by analyzing the current codebase thoroughly, list all existing bugs and technical debt, then systematically fix and enhance it.

Work step by step. After every major change, make sure the project still compiles and runs.